import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'yes-or-no',

  brand: {
    primaryColor: '#3182F6'
  },

  permissions: [],
  webBundleDir: 'dist'
});
